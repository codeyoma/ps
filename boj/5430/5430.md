---
tags: ["ps/boj/gold/5", "ps/boj/gold", "cs/algorithms/deque/ps","cs/algorithms/parsing/ps","cs/algorithms/implementation/ps","cs/algorithms/string/ps","cs/algorithms/data-structures/ps"]
---

# Problem
- [https://www.acmicpc.net/problem/5430](https://www.acmicpc.net/problem/5430)

<br/>

# Logic

### *분석*
- 문제 유형 (알고리즘...)
- 제약 조건 (인풋 크기, 예외 값, 시공간 복잡도...)

### *설계*
1. 알고리즘 선택
2. 자료구조 선택
3. 수도 코드 작성
4. 정합판단
  - `1 ~ 3`과정으로 도출된 로직이 문제를 해결하는가
    - 그렇다 -> **구현**
    - 잘 모르겠다 -> **구현**
    - 아니다 -> 1번부터 다시 점검

### *구현*
- 로직 검증

### *테스트*
- 예외 케이스 고려

<br/>

# My Code
## cpp
```cpp title="boj/5430.cpp"
// https://www.acmicpc.net/problem/5430
// https://codeyoma.github.io/Computer-Science/1-Foundations--and--Theory/Algorithms/ps/boj/5430/5430
#include <iostream>
using namespace std;

#ifdef LOCAL
#    define LOG clog
#else
struct nullstream : ostream {
    nullstream()
        : ostream(nullptr) {}
};
nullstream LOG;
#endif

//--------------------------------------------------------------------------------------------------

#define MAX (1234567891)
#define MIN (-1234567891)

#include <algorithm>
#include <deque>
#include <iostream>

int main() {
    //   logic
    int t;
    cin >> t;

    while (t--) {
        int    n;
        string p, array;

        cin >> p >> n >> array;

        // parse array
        // excute p cmd
        // print result

        deque<string> q;

        {
            int l = 0, r = 0;

            while (l < array.size() && r < array.size()) {
                if (n == 0) {
                    break;
                } else if (array[l] == '[') {
                    l = 1;
                    r = l;
                } else if (array[r] == ',' || array[r] == ']') {
                    string temp = array.substr(l, r - l);
                    q.push_back(temp);
                    l = r + 1;
                    r = l;
                } else {
                    r++;
                }
            }
        }

        bool is_front = true;
        bool is_error = false;
        {
            for (const char& c: p) {
                if (c == 'R') {
                    is_front = !is_front;
                } else {
                    if (q.empty()) {
                        is_error = true;
                    } else if (is_front) {
                        q.pop_front();
                    } else {
                        q.pop_back();
                    }
                }
            }
        }

        {
            if (is_error) {
                cout << "error\n";
            } else {
                cout << "[";
                if (is_front) {
                    for (deque<string>::iterator it = q.begin(); it != q.end(); ++it) {
                        cout << *it;
                        if (next(it) != q.end()) {
                            cout << ',';
                        }
                    }
                } else {
                    for (deque<string>::reverse_iterator it = q.rbegin(); it != q.rend(); ++it) {
                        cout << *it;
                        if (next(it) != q.rend()) {
                            cout << ',';
                        }
                    }
                }
                cout << "]\n";
            }
        }
    }
}

```
